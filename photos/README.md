# Wedding slideshow photos

把要輪播的照片直接上傳到這個 `photos` 資料夾即可。

支援格式：
- `.jpg`
- `.jpeg`
- `.png`
- `.webp`
- `.gif`
- `.avif`

網站會依檔名自然排序，例如：

`001.jpg` → `002.jpg` → `003.jpg`

每張照片顯示 10 秒，播放完會自動循環。

新增或刪除照片後，不需要修改 `index.html`；重新整理網站即可讀取目前資料夾內的照片。
